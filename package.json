{
  "name": "ecommerce-customer-support-rag-n8n",
  "version": "0.1.0",
  "private": true,
  "description": "Portfolio demo: n8n-orchestrated local RAG customer-support bot.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-project.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run check && npm test",
    "evaluate": "node scripts/evaluate-rag.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
